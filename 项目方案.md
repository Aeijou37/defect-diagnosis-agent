# 工业缺陷多模态诊断 Agent — 项目方案

> 把 CXMT 的"缺陷图→分类标签"升级为"缺陷图→自然语言诊断报告"。
> 核心独特性：工业缺陷域知识 + 长尾数据构造 + VLM微调 + 幻觉控制 + 评估框架。

---

## 一、项目定位

### 和 CXMT 工作的关系

| 维度 | CXMT（实习工作） | 本项目（个人项目） |
|---|---|---|
| 输入 | EBeam 晶圆缺陷图 | 工业缺陷图（MVTec AD + 合成） |
| 输出 | 5分类标签（一个数字） | 自然语言诊断报告（类型+位置+严重度+原因+建议） |
| 方法 | LDAM + DINOv2 + 蒸馏 | VLM LoRA微调 + 幻觉控制 + 评估框架 |
| 数据 | 公司私有数据（5455张） | 公开数据 + Diffusion合成 + GPT-4o标注 |
| 长尾处理 | LDAM margin（温和引导） | Diffusion合成稀有类（生成式增强） |

### 为什么有深度（面试讲解点）

1. **不是调 API**——用 LoRA 微调 VLM，让它学会工业缺陷的专业表达
2. **长尾数据构造**——用 Diffusion 合成稀有缺陷，这是 CXMT 报告里提到的未来方向，现在真的去做了
3. **幻觉控制**——工业场景 VLM 编造缺陷是致命的，需要强约束 + 后处理校验
4. **评估框架**——不只跑通，还建立"分类准确率 + 描述质量 + 幻觉率"三维评估

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────┐
│                   用户界面（Gradio）                  │
│  上传缺陷图 → 显示诊断报告 → 追问对话                │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│              诊断 Agent（核心逻辑）                   │
│  1. 图像预处理（标准化/增强）                         │
│  2. VLM 推理（缺陷图→诊断报告）                      │
│  3. 后处理校验（类型校验/格式校验/幻觉过滤）          │
│  4. 多轮对话（用户追问）                              │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│              VLM 模型（LoRA微调后）                    │
│  Qwen-VL-Chat + LoRA adapter                         │
│  视觉编码器（冻结）→ 投影层 → 语言模型（LoRA微调）    │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│              数据管线（离线）                          │
│  MVTec AD 缺陷图 + Diffusion合成 + GPT-4o标注        │
│  → 构造"缺陷图+诊断描述"训练数据                      │
│  → LoRA 微调                                         │
└─────────────────────────────────────────────────────┘
```

---

## 三、数据构造方案（最核心的深度）

### 3.1 数据来源

| 来源 | 数量 | 用途 |
|---|---|---|
| MVTec AD（公开） | ~500张缺陷图 | 真实缺陷图基础 |
| Diffusion合成 | ~1000张 | 稀有缺陷/长尾类补充 |
| GPT-4o标注 | ~1500对（图+描述） | 训练数据标注 |

### 3.2 诊断描述标注格式

每张缺陷图配一段结构化诊断描述：

```
【缺陷类型】气孔（A类）
【位置描述】工件中上部，距上边缘约30mm处
【尺寸估计】直径约2-3mm，共3个独立气孔
【严重等级】2级（少量分散气孔，未达到报废标准）
【可能原因】浇注温度过高导致气体卷入
【处置建议】该工件可继续使用，建议后续批次降低浇注温度
```

### 3.3 长尾数据构造（CXMT经验迁移）

MVTec AD 的缺陷类型分布不均（某些缺陷类只有几十张）。解决方案：

| 策略 | 做法 | 和CXMT的关联 |
|---|---|---|
| Diffusion合成 | 用SD+ControlNet合成稀有类缺陷 | CXMT报告里CLAHE增强失败，这次换生成式方法 |
| 文本增强 | 用GPT-4o对同一缺陷图生成多种表述 | 类似CXMT的"温和引导"——不暴力增强图像，而是丰富描述 |
| 类别平衡 | 按CXMT的经验控制各类样本比例 | LDAM margin思想的迁移 |

### 3.4 为什么不用CXMT的CLAHE/TTA（面试关键点）

> "在CXMT我验证了CLAHE等暴力增强会破坏预训练特征分布（700类recall暴跌12.7pp）。所以这个项目我不用图像层面的暴力增强，而是用**Diffusion生成**和**文本描述多样化**这两种'温和'方式解决长尾问题——这正是CXMT方法学的迁移应用。"

---

## 四、VLM微调方案

### 4.1 微调策略

| 组件 | 策略 | 理由 |
|---|---|---|
| 视觉编码器（CLIP/SigLIP） | **冻结** | 保留通用视觉特征，和CXMT的差异化冻结策略一致 |
| 投影层 | 全量微调 | 适配缺陷域的视觉-语言对齐 |
| 语言模型 | **LoRA微调**（r=8, alpha=16） | 学习工业缺陷的专业表达，省显存 |
| 预训练权重 | Qwen-VL-Chat-Int4 | 4bit基础 + LoRA，16GB显存可训 |

### 4.2 训练数据格式

```json
{
  "id": "defect_001",
  "image": "mvtec/cast_defect_001.png",
  "conversations": [
    {"role": "user", "content": "请分析这张工业缺陷图，给出诊断报告。"},
    {"role": "assistant", "content": "【缺陷类型】气孔（A类）\n【位置描述】工件中上部，距上边缘约30mm处\n【尺寸估计】直径约2-3mm，共3个独立气孔\n【严重等级】2级\n【可能原因】浇注温度过高导致气体卷入\n【处置建议】该工件可继续使用，建议后续批次降低浇注温度"}
  ]
}
```

### 4.3 训练配置

| 参数 | 值 | 说明 |
|---|---|---|
| 基础模型 | Qwen-VL-Chat-Int4 | 4bit量化 |
| LoRA rank | 8 | 低秩矩阵维度 |
| LoRA alpha | 16 | 缩放因子 |
| 学习率 | 2e-4 | LoRA用较大学习率 |
| Batch size | 2 | 16GB显存限制 |
| Epochs | 3-5 | 小数据集不宜过多 |
| 训练框架 | PEFT + Transformers | HuggingFace生态 |

---

## 五、幻觉控制方案（工业场景关键）

### 5.1 三层幻觉防护

| 层级 | 方法 | 作用 |
|---|---|---|
| **输入层** | 图像质量检查（分辨率/亮度/对比度） | 拒绝质量太差的图，避免VLM"猜" |
| **Prompt层** | 强约束System Prompt | 限定输出格式+缺陷类型必须在预定义列表内 |
| **输出层** | 后处理校验 | 解析输出→校验缺陷类型合法性→校验等级范围→过滤编造内容 |

### 5.2 System Prompt 设计

```
你是一个工业缺陷诊断专家。分析缺陷图像时必须遵守：
1. 只能从以下缺陷类型中选择：气孔(A)/砂眼夹渣(B)/线性收缩(Ca)/羽毛状收缩(Cb)/海绵状收缩(Cc)/裂纹(D)/热断裂(E)
2. 如果无法确定缺陷类型，必须回答"无法确定，建议人工复检"
3. 严重等级必须是1-5的整数
4. 不得编造图像中不存在的缺陷
5. 输出必须严格按【缺陷类型】【位置描述】【尺寸估计】【严重等级】【可能原因】【处置建议】格式
```

### 5.3 后处理校验代码逻辑

```python
VALID_DEFECT_TYPES = {"气孔", "砂眼夹渣", "线性收缩", "羽毛状收缩", "海绵状收缩", "裂纹", "热断裂"}

def validate_diagnosis(report: str) -> dict:
    parsed = parse_report(report)  # 解析结构化输出
    errors = []
    
    if parsed["defect_type"] not in VALID_DEFECT_TYPES:
        errors.append(f"无效缺陷类型: {parsed['defect_type']}")
    
    if not (1 <= parsed["severity"] <= 5):
        errors.append(f"无效等级: {parsed['severity']}")
    
    if "无法确定" in report and not parsed["defect_type"]:
        return {"valid": True, "note": "模型主动拒绝判断，符合安全策略"}
    
    return {"valid": len(errors) == 0, "errors": errors, "parsed": parsed}
```

---

## 六、评估框架

### 6.1 三维评估指标

| 维度 | 指标 | 评估方法 |
|---|---|---|
| **分类准确率** | 缺陷类型识别准确率 | 和MVTec AD ground truth对比 |
| **描述质量** | 诊断报告质量评分 | GPT-4o评分(1-5) + 人工评分(1-5) |
| **幻觉率** | 编造内容占比 | 人工检查：描述中有多少是图像中不存在的 |

### 6.2 评估流程

```
测试集（100张缺陷图）
    ↓
VLM 生成诊断报告
    ↓
评估1: 缺陷类型 vs GT → 准确率
评估2: 诊断报告 → GPT-4o 评分 → 描述质量
评估3: 诊断报告 → 人工检查 → 幻觉率
    ↓
三维评估报告
```

### 6.3 和CXMT的对比评估

| 指标 | CXMT（纯分类） | 本项目（VLM诊断） |
|---|---|---|
| 分类准确率 | 100%（蒸馏） | 目标>90% |
| 输出信息量 | 1个类别标签 | 6维度诊断报告 |
| 可解释性 | 低（只有数字） | 高（自然语言解释） |
| 业务价值 | 自动分类 | 自动诊断+处置建议 |

---

## 七、3-4周执行节奏

### 第1周：数据构造

| 天 | 任务 | 产出 |
|---|---|---|
| 1-2 | 下载MVTec AD + 熟悉缺陷类型分布 | 数据集就绪 |
| 3-4 | 用GPT-4o为缺陷图生成诊断描述标注 | 1500对训练数据 |
| 5-7 | 用SD+ControlNet合成稀有缺陷图 | 长尾数据补充 |

### 第2周：VLM微调

| 天 | 任务 | 产出 |
|---|---|---|
| 1-2 | 搭建LoRA微调环境 + 数据格式转换 | 训练管线 |
| 3-5 | LoRA微调Qwen-VL（3-5 epoch） | 微调模型 |
| 6-7 | 初步评估 + 超参调整 | 基线结果 |

### 第3周：幻觉控制 + 评估框架

| 天 | 任务 | 产出 |
|---|---|---|
| 1-2 | System Prompt设计 + 后处理校验 | 幻觉防护 |
| 3-4 | 搭建三维评估框架（准确率+描述质量+幻觉率） | 评估管线 |
| 5-7 | 评估 + 分析 + 优化 | 评估报告 |

### 第4周：Demo + 部署 + 文档

| 天 | 任务 | 产出 |
|---|---|---|
| 1-2 | Gradio界面（上传图→诊断报告→追问） | 前端 |
| 3 | 录demo视频/截图 | 展示材料 |
| 4 | README + 技术博客 | 文档 |
| 5 | HuggingFace Space部署 | 在线demo |
| 6-7 | 优化 + 收尾 | 完整项目 |

---

## 八、项目文件结构

```
defect-diagnosis-agent/
├── README.md
├── requirements.txt
├── configs/
│   ├── defect_types.json          # 缺陷类型定义
│   └── training_config.yaml       # LoRA训练配置
├── data/
│   ├── prepare_mvtec.py           # MVTec AD数据预处理
│   ├── generate_labels.py         # GPT-4o生成诊断描述
│   ├── synthesize_defects.py      # Diffusion合成稀有缺陷
│   └── format_training_data.py    # 格式化为训练数据
├── src/
│   ├── vlm.py                     # VLM加载+推理
│   ├── diagnosis_agent.py         # 诊断Agent（预处理+VLM+后处理）
│   ├── hallucination_guard.py     # 幻觉控制（Prompt+校验）
│   └── app.py                     # Gradio前端
├── training/
│   ├── train_lora.py              # LoRA微调脚本
│   └── merge_lora.py              # 合并LoRA权重
├── evaluation/
│   ├── eval_accuracy.py           # 分类准确率评估
│   ├── eval_description.py        # 描述质量评估（GPT-4o评分）
│   ├── eval_hallucination.py      # 幻觉率评估
│   └── generate_report.py         # 三维评估报告
└── tests/
    ├── test_vlm.py
    ├── test_hallucination.py
    └── test_agent.py
```

---

## 九、简历写法

> **工业缺陷多模态诊断 Agent** — 基于 Qwen-VL LoRA 微调的工业缺陷自动诊断系统。将传统缺陷分类（输出类别标签）升级为自然语言诊断报告生成（输出缺陷类型+位置+严重等级+原因分析+处置建议）。
>
> - **长尾数据构造**：用 Diffusion 合成稀有缺陷样本，迁移 CXMT 的"温和引导而非暴力施压"方法学（避免 CLAHE 等暴力增强破坏特征分布）
> - **VLM 微调**：LoRA 微调 Qwen-VL-Chat（r=8），视觉编码器冻结，语言模型学习工业缺陷专业表达
> - **幻觉控制**：三层防护（输入质量检查 + 强约束 System Prompt + 输出后处理校验），确保工业场景安全性
> - **三维评估**：分类准确率 + 描述质量（GPT-4o评分）+ 幻觉率，构建完整评估框架
> - **部署**：4bit 量化 + Gradio 界面，16GB 显存可运行

---

## 十、面试讲解要点

### Q1：这个项目和你在 CXMT 的工作有什么关系？
> "CXMT 做的是缺陷分类——输入图，输出一个类别数字。但产线工程师需要的不是数字，而是诊断报告。这个项目把分类升级为诊断报告生成。数据构造时我迁移了 CXMT 的长尾方法学——不用 CLAHE 暴力增强（在 CXMT 已验证有害），改用 Diffusion 生成式合成。"

### Q2：为什么不直接用 GPT-4o/VLM 零样本推理，要微调？
> "零样本 VLM 不懂工业缺陷的专业术语和诊断格式。比如它可能说'这里有个黑点'，而不是'2级气孔，建议降低浇注温度'。LoRA 微调让它学会工业缺陷的专业表达，同时省显存（只训练 0.5% 的参数）。"

### Q3：幻觉控制为什么在工业场景特别重要？
> "VLM 可能编造图像中不存在的缺陷——在消费级应用里这只是体验问题，在工业场景里可能导致合格工件被报废、或缺陷工件被放过。所以我设计了三层防护：输入层拒绝低质量图、Prompt 层限定输出范围、输出层校验类型合法性。"

### Q4：Diffusion 合成缺陷和真实缺陷的差距怎么处理？
> "合成缺陷的分布和真实缺陷有差距。我的处理：①用 ControlNet 约束合成缺陷的形态（边缘/深度条件），让它更接近真实形态；②合成数据只用于补充稀有类，不替换真实数据；③评估时在真实 MVTec AD 测试集上评估，不和合成数据混。"

### Q5：你的评估框架和别人有什么不同？
> "别人通常只看'生成文本通不通顺'，我设计了三维评估：①分类准确率（和 GT 对比，可量化）；②描述质量（GPT-4o 评分，半量化）；③幻觉率（人工检查编造内容占比）。这三维对应工业场景的三个核心需求：判对、说清、不编。"
